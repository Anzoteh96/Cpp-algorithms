#include <bits/stdc++.h>

using namespace std;

typedef long long int ll;

int main () {
    int n;
    scanf("%d", &n);
    vector<int>s(n);
    vector<int>t(n);
    for (int i = 0; i < n; ++i) {
        scanf("%d", &s[i]);
    }
    for (int i = 0; i < n; ++i) {
        scanf("%d", &t[i]);
    }
}
